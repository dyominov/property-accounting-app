describe('documents page', () => {
  beforeEach(() => {
    cy.login();
    cy.visit('/');
  });

  it('in main menu Documents is active', () => {
    
  });
  it('table name is Documents', () => {
    
  });

  it('toolbar links to /add-supply', () => {
    
  });

  it('toolbar links to /pin-ma', () => {
    
  });

  it('links to filter', () => {
    
  });

  it('links to search settings', () => {
    
  });

  it('table columns as expected for document table', () => {
    
  });
  it('table rows is checkable', () => {
    
  });
  it('if selected document(s) is not realized appear "Realize" button in toolbar', () => {
    
  });
  

});